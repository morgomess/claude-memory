# Reference

Standing context that doesn't change often: client facts, brand constants, recurring rules Claude should orient against.

This is the slow-moving counterpart to `/sessions/`. Update when a fact changes, not every session.
