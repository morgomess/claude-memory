# SOPs

One `.md` per standard operating procedure. Name by what it does: `aios-content-generation.md`, `client-onboarding.md`.

Keep each self-contained. If a procedure changes, edit the file - git history is the changelog.
