# Data

Small structured files only - json, csv. Things Claude reads or updates as data rather than prose.

Heavy or binary files (images, video, large datasets) go to R2. Relational/queryable data goes to Airtable.
